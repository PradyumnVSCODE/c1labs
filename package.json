import express from "express";

const app = express();
app.use(express.json());

app.get("/test", (req, res) => {
  res.json({ ok: true });
});

app.post("/chat", (req, res) => {
  res.json({ reply: "Chat endpoint works" });
});

app.listen(3000, () => {
  console.log("SERVER RUNNING CLEAN");
});
